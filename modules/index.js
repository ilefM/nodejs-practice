// A module that I have declared must be imported with "./" or "../"
// (differences between that and third party or built-in modules)
const someVariables = require("./variables")
console.log(someVariables.ilef)
